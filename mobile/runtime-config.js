window.ANAV1_CONFIG = window.ANAV1_CONFIG || {
  // Leave blank for local /mobile-preview testing.
  // For Android/iPhone builds, set this to your Railway HTTPS URL with no trailing slash.
  apiBaseUrl: "",
};
